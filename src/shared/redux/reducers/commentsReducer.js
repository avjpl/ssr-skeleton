import * as ActionTypes from '../constants';

const initialState = {};

const comments = (state = initialState, action = '') => {
  switch (action.type) {
    default:
      return state;
  }
};

export default comments;
